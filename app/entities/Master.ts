export class Master {
    masterId: number;
    masterNumber: string;
    constructor(masterId: number, masterNumber: string) {
        this.masterId = masterId;
        this.masterNumber = masterNumber;
    }
}